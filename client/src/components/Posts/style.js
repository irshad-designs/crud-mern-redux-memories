import { makeStyles } from "@material-ui/styles";

export default makeStyles((t) => ({
  mainContainer: {
    display: "flex",
    alignItems: "center",
  },
  smMargin: {
    margin: 2,
  },
  actionDiv: {
    textAlign: "center",
  },
}));
